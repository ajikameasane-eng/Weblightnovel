backend:
  name: github
  repo: ajikameasane-eng/Website   # repo của cậu
  branch: main
  base_url: https://ajikameasane-eng.github.io/Website
  auth_endpoint: /api/auth
  app_id: <Ov23liia3lC0iLCwaJqR>
  client_id: <Ov23liia3lC0iLCwaJqR>

media_folder: "assets/uploads"          # Nơi lưu ảnh thật trong repo
public_folder: "/assets/uploads"        # Đường dẫn công khai để hiển thị ảnh

collections:
  - name: "stories"
    label: "Truyện"
    folder: "stories"
    create: true
    slug: "{{slug}}"                     # Tên file tự động theo tiêu đề
    fields:
      - { label: "Tiêu đề", name: "title", widget: "string" }
      - { label: "Tác giả", name: "author", widget: "string", default: "Asane" }
      - { label: "Ngày đăng", name: "date", widget: "datetime" }
      - { label: "Ảnh bìa", name: "thumbnail", widget: "image" }
      - { label: "Mô tả ngắn", name: "description", widget: "text" }
      - { label: "Nội dung", name: "body", widget: "markdown" }
